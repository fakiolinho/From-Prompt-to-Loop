#!/usr/bin/env bash
set -uo pipefail
# Non-zero when production failures are not yet in the golden set. Wire to your logs/store.
echo "new production failures to fold into the golden set (wire your log source)"; exit 1

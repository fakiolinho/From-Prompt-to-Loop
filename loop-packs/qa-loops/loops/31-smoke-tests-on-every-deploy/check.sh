#!/usr/bin/env bash
set -uo pipefail
# Non-zero when post-deploy smoke tests fail. Wire to your deploy target and smoke suite.
echo "run smoke tests against the new release (wire your deploy target)"; exit 1

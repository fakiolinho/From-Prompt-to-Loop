#!/usr/bin/env bash
set -uo pipefail
# Non-zero when UI tests fail on a locator. Wire to your UI test runner (Playwright/Cypress).
echo "UI test selector failures to review (wire your UI test run)"; exit 1
